export interface Theme {
  id: string;
  name: string;
  backgroundColor: string;
  backgroundImage?: string;
  primaryColor: string;
  secondaryColor: string;
  textColor: string;
  fontFamily: string;
}
